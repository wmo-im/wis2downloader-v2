= Руководство по быстрому началу работы с WIS2 Downloader
:toc: left
:toc-title: Содержание
:toclevels: 2
:icons: font
:source-highlighter: highlight.js

[NOTE]
====
Этот документ был автоматически переведён с английского языка с помощью Claude AI.
Термины из области WMO/метеорологии должны быть проверены носителем языка перед
использованием в производственной среде. Смотрите link:../en/[оригинал на английском] для авторитетной версии.
====

Запустите WIS2 Downloader и начните загружать данные за несколько минут.

== Предварительные требования

* https://docs.docker.com/get-docker/[Docker] 20.10+
* https://docs.docker.com/compose/install/[Docker Compose] 2.0+
* Сетевой доступ к WIS2 Global Broker (порт 443)

== 1. Загрузка и распаковка

Загрузите архив https://github.com/World-Meteorological-Organization/wis2downloader/releases/latest[последнего релиза] и распакуйте его:

[source,bash]
----
tar -xzf wis2downloader-*.tar.gz
cd wis2downloader-*
----

TIP: Для работы с последней версией разработки склонируйте репозиторий: `git clone https://github.com/World-Meteorological-Organization/wis2downloader.git`

== 2. Настройка

Запустите скрипт настройки для создания файла `.env` со случайно сгенерированными секретами:

[source,bash]
----
bash setup.sh
----

Скрипт копирует `default.env` в `.env` и генерирует случайные значения для `FLASK_SECRET_KEY` и `REDIS_PASSWORD`. Проверьте файл `.env` и при необходимости скорректируйте параметры — значения по умолчанию подходят для развёртывания на одной машине.

IMPORTANT: Храните файл `.env` в безопасном месте. Он содержит конфиденциальные учётные данные и не должен добавляться в систему контроля версий.

== 3. Установка плагина Docker для Loki

WIS2 Downloader использует https://grafana.com/docs/loki/latest/send-data/docker-driver/[драйвер Docker Loki] для отправки журналов. Установите его перед запуском стека:

[source,bash]
----
ARCH=$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/')
docker plugin install grafana/loki-docker-driver:3.6.7-${ARCH} --alias loki --grant-all-permissions
----

Убедитесь, что плагин включён:

[source,bash]
----
docker plugin ls
----

Вы должны увидеть `loki:latest` со значением `ENABLED` равным `true`.

NOTE: При возникновении ошибки во время установки проверьте архитектуру командой `uname -m`. Наиболее распространённые значения: `x86_64` (amd64) и `aarch64` (arm64).

== 4. Настройка каталога загрузок

Контейнеры работают от имени непривилегированного пользователя (UID 10001, GID 988 по умолчанию). В Linux необходимо настроить каталог загрузок перед запуском:

[source,bash]
----
sudo groupadd -g 988 wis2
sudo useradd -u 10001 -g wis2 -M -s /usr/sbin/nologin wis2
mkdir -p downloads
sudo chown wis2:wis2 downloads
----

NOTE: Если UID 10001 или GID 988 конфликтуют с существующими пользователями системы, переопределите их, задав `WIS2DOWNLOADER_UID` и `WIS2DOWNLOADER_GID` в файле `.env` перед запуском `docker compose up`. Используйте соответствующие значения в командах выше. Если вы изменяете `WIS2DOWNLOADER_UID` или `WIS2DOWNLOADER_GID` после того, как образы уже были собраны, пересоберите их командой `docker compose build` перед перезапуском.

NOTE: На macOS и Windows (Docker Desktop) этот шаг не требуется — Docker Desktop автоматически управляет правами доступа к томам.

Если создать соответствующего пользователя невозможно, обратитесь к разделу link:admin-guide.html#_volume_permissions[Права доступа к томам] Руководства администратора, где описаны альтернативные подходы на основе ACL.

== 5. Запуск сервисов

[source,bash]
----
docker compose up -d
----

Эта команда запускает все сервисы: менеджер подписок, MQTT-подписчики (Global Broker Франции и Бразилии), рабочие процессы Celery (очереди высокого приоритета, малых и больших файлов), Redis, Prometheus, Grafana и Loki.

Проверьте, что все сервисы работают:

[source,bash]
----
docker compose ps
----

Дождитесь успешного прохождения проверки работоспособности `subscription-manager` (около 30 секунд), затем убедитесь, что сервис запущен:

[source,bash]
----
curl http://localhost:5002/health
----

== 6. Создание подписки

=== Через веб-интерфейс (рекомендуется)

Откройте http://localhost:8080 в браузере.

Используйте *Каталог* или *Дерево тем* для просмотра доступных наборов данных WIS2, выберите тему, укажите каталог сохранения и нажмите *Подписаться*.

=== Через API

[source,bash]
----
curl -X POST http://localhost:5002/subscriptions \
  -H "Content-Type: application/json" \
  -d '{
    "topic": "cache/a/wis2/+/data/core/weather/surface-based-observations/#",
    "target": "surface-obs"
  }'
----

Эта команда создаёт подписку на все наземные наблюдения от любого центра данных и сохраняет файлы в `downloads/surface-obs/`.

== 7. Просмотр загруженных файлов

Файлы сохраняются в каталоге `downloads/`, упорядоченные по дате:

[source,bash]
----
ls -la downloads/surface-obs/
----

Следите за активностью загрузок в Grafana по адресу http://localhost:3000 (учётные данные по умолчанию: `admin` / `admin`).

IMPORTANT: Смените пароль Grafana по умолчанию после первого входа.

== Дальнейшие шаги

[cols="1,3"]
|===
|Руководство |Содержание

|link:user-guide.html[Руководство пользователя]
|Работа с веб-интерфейсом, создание и управление подписками, фильтрация данных

|link:admin-guide.html[Руководство администратора]
|Повышение безопасности, масштабирование, переменные окружения, мониторинг, обслуживание

|link:api-reference.html[Справочник API]
|Полная документация REST API; интерактивный интерфейс Swagger по адресу `http://localhost:5002/swagger`

|link:developer-guide.html[Руководство разработчика]
|Архитектура системы, внутреннее устройство модулей, расширение системы
|===
